import { TkHighlightDirective } from './tk-highlight.directive';

describe('TkHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TkHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
