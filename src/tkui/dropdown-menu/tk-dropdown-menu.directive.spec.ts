import { TkDropdownMenuDirective } from './tk-dropdown-menu.directive';

describe('TkDropdownMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new TkDropdownMenuDirective();
    expect(directive).toBeTruthy();
  });
});
