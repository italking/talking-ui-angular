import { TkTabDirective } from './tk-tab.directive';

describe('TkTabDirective', () => {
  it('should create an instance', () => {
    const directive = new TkTabDirective();
    expect(directive).toBeTruthy();
  });
});
