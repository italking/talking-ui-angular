import { TkWindowContentDirective } from './tk-window-content.directive';

describe('TkWindowContentDirective', () => {
  it('should create an instance', () => {
    const directive = new TkWindowContentDirective();
    expect(directive).toBeTruthy();
  });
});
