import { RightMenuDirective } from './right-menu.directive';

describe('RightMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new RightMenuDirective();
    expect(directive).toBeTruthy();
  });
});
