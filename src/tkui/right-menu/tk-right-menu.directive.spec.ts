import { TkRightMenuDirective } from './tk-right-menu.directive';

describe('TkRightMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new TkRightMenuDirective();
    expect(directive).toBeTruthy();
  });
});
