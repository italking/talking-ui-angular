import { TkBaseTabDirective } from './tk-base-tab.directive';

describe('TkBaseTabDirective', () => {
  it('should create an instance', () => {
    const directive = new TkBaseTabDirective();
    expect(directive).toBeTruthy();
  });
});
