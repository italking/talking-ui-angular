import { TkBaseTabHeaderDirective } from './tk-base-tab-header.directive';

describe('TkBaseTabHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TkBaseTabHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
