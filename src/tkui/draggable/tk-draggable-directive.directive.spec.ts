import { TkDraggableDirectiveDirective } from './tk-draggable-directive.directive';

describe('TkDraggableDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TkDraggableDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
