import { TkBaseTableDirective } from './tk-base-table.directive';

describe('TkBaseTableDirective', () => {
  it('should create an instance', () => {
    const directive = new TkBaseTableDirective();
    expect(directive).toBeTruthy();
  });
});
